точка входа ./htdocs/index.php
авторизация:
admin 123456
userp 123456
файл композера находится ./yii/composer.json
файл настроек ./yii/common/config/params.php(пагинация и изображение по умолчанию)
подключение к базе ./yii/common/config/main-local.php

так как проект писал с первого раза, вначале напортачил несколько раз и git работал не правильно, из-за этого по факту все перенес в другую директорию и закомитил(комиты разработки в итоге не видно)

до ума все не доводил, так что проверка прав пользователй на доступ и редактирование выполнена только с личным кабинетом

по итогу не победил gridview(не переносит слова, хотя размер для вывода изображений удалось задать, а вот перенос слов так и не получилось)

попробовал все вывести через стандартные виджеты и простые таблицы

в коде остался местами закоментированный "мусор", где я разбирался как и что работает

ну и скорее всего есть еще много косяков так как это мой первый проект с использованием фреймворка







Необходимо реализовать гостевую книгу на php, удовлетворяющую следующим критериям:

    Информация о пользователях и записях в гостевой хранится в базе данных MySQL.
сделано
    Самые последние записи выводятся сверху.
сделано
    Реализована пагинация (постраничный вывод). Если в книге больше X записей, выводятся последние X, далее формируется ссылка на следующую страницу. Должна быть возможность редактировать параметр X в одном месте. (config-файл).
сделано ./yii/common/config/params.php
    Регистрация/авторизация пользователей. В случае авторизации, при написании сообщения в гостевой, автоматически подставляется имя пользователя.
сделано
    Возможность анонимного написания сообщений. В этом случае можно указать свое имя, но при выводе будет сделана соответствующая пометка, что комментарий оставлен анонимно.
сделано isguest а записи автор ставится null и потом проверяется
    Разделение зарегистрированных пользователей на админов и простых пользователей. Админы могут удалять/редактировать другие сообщения.
сделано

Дополнительно можно реализовать:

    Авторизацию через соц.сети (twitter/facebook/vk)
не сделано, сайт на локале, прочитал документацию yiisoft/yii2-authclient 
    Возможность комментирования сообщений в гостевой.
сделана, все записи хранятся в 1 таблице и по полю parents определяется комментарий или запись
    Captcha (картинка для проверки, что вы не робот) при комментировании анонимно
сделано, стандартная капча yii
    Возможность админу отвечать на сообщения со специальной пометкой (от администрации).
сделано, при выводе проверяется автор админ и если true то ставится пометка
    Возможность управлять своим профилем. Смена аватарки. Аватар по-умолчанию.
сделано, реализована возможность админу менять любой профиль, пользователю только свой
    Возможность админу блокировать пользователей.
сделано админ может заблокировать и назначить другого пользователя админом
    Встроенный антимат/антиспам фильтр.
не сделано, сайт на локале, прочитал документацию yii2-antispam
